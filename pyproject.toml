[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "durable-media-publishing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []
[project.scripts]
media-pipeline = "durable_media.cli:main"
[tool.pytest.ini_options]
pythonpath = ["src"]
